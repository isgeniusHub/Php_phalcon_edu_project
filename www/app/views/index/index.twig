<!doctype html>
<html lang="ru">
    <head>
    	<meta charset="utf-8">
    	<title>Index page</title>
    </head>
    <body>

    <p>{{ authTitle }}</p>
    <p>{{ name }}</p>
    <ul>
    	{% for user in users %}
    		<li>{{ user }}</li>
    	{% endfor %}
    </ul>
    </body>
</html>